/**
 * Personalize → get-started (shared module selectors).
 *
 * DOM captures for locator work live under **`data/dom/Personalize/`** (e.g. `personalize-projects.html`, `experiences.html`).
 * Add flow-specific maps in **`projects/Personalize/<module>/selectors/<flow-id>.selectors.ts`** as you add `*.flow.json`.
 */

export const CLICK_SELECTORS: Record<string, string> = {};
