/**
 * use-task-filter — Tasks left filter panel targets live in `data/dom/CMS/workflows/tasks-filter.html`.
 *
 * Extracted UI labels (accordion section titles):
 * - "Filters" (section header)
 * - "Users" (doc says "By Users")
 * - "Action" (doc: "By Action")
 * - "Access Request" (present in app; not in use-task-filter doc bullet list)
 * - "Workflow Stage" (doc: "By Workflow Stages")
 * - "Content Type" (present in app; not in doc bullet list)
 *
 * Action checkboxes: Approved, Pending, Rejected (matches doc examples).
 */
export const CLICK_SELECTORS: Record<string, string> = {};
export const INPUT_SELECTORS: Record<string, string> = {};
