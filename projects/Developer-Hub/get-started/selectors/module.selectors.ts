/**
 * Shared selectors for Developer-Hub flows under `get-started`.
 * Add `CLICK_SELECTORS` / `INPUT_SELECTORS` entries as executable flows land in
 * `projects/Developer-Hub/get-started/flows/*.flow.json`.
 */

export const CLICK_SELECTORS: Record<string, string> = {};

export const INPUT_SELECTORS: Record<string, string> = {};
