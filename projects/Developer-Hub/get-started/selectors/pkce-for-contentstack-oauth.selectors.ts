/**
 * PKCE for Contentstack OAuth — Developer Hub OAuth tab (doc §Enabling PKCE in Contentstack).
 * Most targets are handled in `rules/core/actionRules.ts` for this flow id; keep hash nav here.
 */

export const CLICK_SELECTORS: Record<string, string> = {
  "Developer Hub app settings OAuth nav (doc step)": "#oauth",
};

export const INPUT_SELECTORS: Record<string, string> = {};
