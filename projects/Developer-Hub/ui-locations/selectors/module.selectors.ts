/**
 * Shared selectors for Developer Hub UI Locations doc flows (`module: ui-locations`).
 * Reuses boilerplate locator map (same Developer Hub product surface).
 */
export { CLICK_SELECTORS, INPUT_SELECTORS } from "../../get-started/selectors/marketplace-app-boilerplate.selectors";
