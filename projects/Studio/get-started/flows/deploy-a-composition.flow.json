{
  "id": "deploy-a-composition",
  "project": "Studio",
  "module": "get-started",
  "stage": "main",
  "source": "https://www.contentstack.com/docs/studio/deploy-a-composition",
  "type": "executable",
  "use": ["login"],
  "automationNotes": "Doc-only steps (no navigation invented — doc never describes how to reach the canvas, so this flow starts from wherever `use: [\"login\"]` leaves off and expects the Deploy button to already be visible; if it isn't, that itself documents the doc's missing prerequisite). Doc text verbatim: 'The Deploy button sits next to Save. Clicking it opens the Deploy modal: pick which environments to publish to, decide whether to send referenced entries along with the composition, and confirm.' Modal title per doc: 'Deploy Template'. Doc never names the confirm control 'Publish' — that label was an assumption in the old flow and has been removed; environment pick and confirm are generic actions with no strict label verification.",
  "steps": [
    {
      "action": "verify",
      "target": "Studio canvas editor Deploy button visible (doc step)",
      "timeoutMs": 90000,
      "expected": { "labelEquals": "Deploy", "labelMatch": "contains" }
    },
    { "action": "click", "target": "Studio canvas editor Deploy button (doc step)", "timeoutMs": 90000 },
    {
      "action": "verify",
      "target": "Studio Deploy modal heading (doc step)",
      "timeoutMs": 60000,
      "expected": { "within": "Modal", "modalTitle": "Deploy Template", "labelEquals": "Deploy Template", "labelMatch": "contains" }
    },
    { "action": "click", "target": "Studio Deploy modal pick environment (doc step)", "timeoutMs": 60000 },
    { "action": "click", "target": "Studio Deploy modal confirm (doc step)", "timeoutMs": 120000 }
  ]
}
